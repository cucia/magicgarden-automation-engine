services:
  magicgarden:
    build: .
    cpus: "1.5"
    mem_limit: "1g"
    ports:
      - "5900:5900"
    env_file:
      - .env
    volumes:
      - magicgarden_browser_profile:/app/browser-profile

volumes:
  magicgarden_browser_profile:
